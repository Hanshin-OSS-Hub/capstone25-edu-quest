spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        base-url: "https://generativelanguage.googleapis.com/v1beta/openai/"
        options:
          model: "gemini-2.5-flash-lite"
          temperature: 0.0
        completions-path: "/chat/completions"
  application:
    name: backend
  flyway:
    baseline-on-migrate: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_ADDRESS}
    password: ${EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  profiles:
    active:
      - dev
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

jwt:
  secret_key: ${JWT_SECRET_KEY}
  access_key_expiration: 3600000
  refresh_key_expiration: 1209600000

admin:
  id: ${ADMIN_ID}
  email: ${ADMIN_EMAIL}
  password: ${ADMIN_PASSWORD}

frontend:
  url: ${FRONTEND_URL:http://localhost:3000}

app:
  backend:
    base-url: ${BACKEND_URL:http://localhost:8080}
  mail:
    token-expiration-millis: ${MAIL_TOKEN_EXPIRATION_MILLIS:600000}
